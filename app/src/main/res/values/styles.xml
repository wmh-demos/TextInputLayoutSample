<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextStatusNormal">
        <item name="android:textColor">@color/color_c1</item>
        <item name="android:textSize">@dimen/text_size_t7</item>
    </style>
    <style name="EditTextStatusError">
        <item name="android:textColor">@color/color_c2</item>
        <item name="android:textSize">@dimen/text_size_t7</item>
    </style>
</resources>
